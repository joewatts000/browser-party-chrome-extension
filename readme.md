# browser party extension

`bun install` - installs packages
`bun run dev`
`bun run build`
`bun run lint`
`bun run lint:fix`
`bun run format`

## edit the config in `./src/utils/effects.js` to change the animations
